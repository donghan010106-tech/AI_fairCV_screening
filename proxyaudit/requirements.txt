# Core (required). The whole audit + paper figures run on these alone.
# Preferred install: pip install -e .  (reads pyproject.toml)

numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12

# Streamlit & visualization
streamlit>=1.28
plotly>=5.0
kaleido>=0.2

# ML & statistics
scipy>=1.8
joblib>=1.3
xgboost>=2.0

# Optional extras:
# pip install -e ".[app]"        -> streamlit (interactive auditor)
# pip install -e ".[datasets]"   -> shap, xgboost, datasets (real FairJob loader)
# pip install -e ".[dev]"        -> pytest, pytest-cov, ruff

shap>=0.42
datasets>=2.12
